# Route of Sky 작업 규칙

이 파일은 저장소에서 작업하는 모든 에이전트와 기여자의 공통 규칙입니다. 기능 완성보다 재현 가능한 변경·검증·기록을 우선합니다.

## Git과 PR

- `main`에는 직접 커밋하거나 푸시하지 않습니다.
- 새 작업은 최신 `main`에서 `codex/<작업명>` 브랜치로 시작합니다.
- 작업 순서는 **기준선·작업 일지 → 기능 단위 커밋 → 사후 측정·문서 → 검증 → 푸시 → PR → 자체 리뷰 → Merge commit 병합**입니다.
- 다음 브랜치는 이전 PR이 Merge commit으로 병합된 최신 `main`에서 생성합니다.
- PR 본문은 한국어로 목표, 변경, 테스트, Before/After 수치, 위험, 롤백 조건을 포함합니다.
- PR에는 diff·테스트·측정 근거·문서 누락을 확인한 자체 리뷰 체크리스트를 코멘트로 남깁니다.
- 보호 규칙 또는 외부 승인이 필요한 경우 우회하지 않고 승인을 요청합니다.

## 작업 일지와 문서

- 일반 작업은 `docs/worklogs/<작업명>.md`에 작업 일지를 남기고 `docs/worklogs/_template.md` 양식을 따릅니다.
- 성능 작업은 시기별 폴더로 나누지 않고 `docs/performance/`에 하나의 연속된 기록으로 보관합니다.
  - `runs/`: Before/After 원본 JSON과 중앙값. 파일명은 작업·시나리오 기반으로 짓습니다.
  - `assets/`: 동일 조건의 시각 캡처·차트
  - `comparison.md`, `measurement-protocol.md`, `optimization-log.md`, `case-study.md`: 전체 비교·재현 조건·시간순 기록·공개 사례 연구
  - 성능 작업 일지는 `docs/worklogs/performance-optimization.md`에 이어 쓰고, 변경 가설·수치·판정·롤백 조건을 해당 항목에 남깁니다.
- 성능 표에는 반드시 **Before → After, 절대 차이, 개선율, 목표 판정**을 기록합니다.
- 시간·용량·호출 수 개선율은 `(Before - After) / Before × 100`으로 계산합니다.
- 목표 미달·실패 수치도 숨기지 않습니다. 기능 코드가 롤백되면 실패 원인, 대안, 롤백 사실을 문서와 PR에 남깁니다.

## 성능 측정과 개인정보

- 공식 GPU 성능 수치는 실제 GPU가 활성화된 로컬 Chrome/Chromium, 1365×768, HTTP 캐시 비활성화, 동일 시나리오 3회 중앙값으로 확정합니다. 데스크톱과 CPU ×4 저사양을 분리 기록합니다.
- SwiftShader, llvmpipe, software renderer 결과는 공식 수치로 사용하지 않습니다.
- 외부 타일·외부 Weather API의 변동은 p50/p95로 관찰하되, 단독 회귀 판정 근거로 사용하지 않습니다.
- API 키, 환경 변수, 사용자 위치, 식별자, 원시 URL 쿼리, 원시 GPU 모델·드라이버 정보는 코드·로그·문서·스크린샷·PR에 기록하지 않습니다.
- 개발 전용 성능 수집은 로컬 개발 환경에서만 활성화하고 Preview·Production 사용자 정보를 수집·저장·전송하지 않습니다.

## 구현과 검증

- 한 브랜치에서는 한 가지 가설과 한 가지 검증 가능한 변경만 다룹니다.
- 렌더링 최적화는 실제 trace로 병목을 확인한 뒤에만 적용합니다. 평균 FPS가 아닌 프레임 p95와 기능·시각 품질을 함께 판정합니다.
- High 품질의 기준 시각 효과는 사전 합의 없이 낮추지 않습니다. Medium/Low 품질 조정은 수치와 시각 검증 근거가 있을 때만 허용합니다.
- 성능 구현은 목표를 달성하고 다른 시나리오가 5% 이상 회귀하지 않으며 기능 검증을 통과할 때만 유지합니다. 하나라도 실패하면 구현을 롤백하고 문서만 병합합니다.
- 변경 전후로 해당 범위의 단위 테스트와 Playwright E2E를 실행합니다. 성능 작업은 build, `perf:budget`, 실제 GPU 3회 측정, High/Medium/Low의 Rain/Storm/Snow 시각 확인을 추가합니다.
- 기능 검증에는 지역 변경, 시간 슬라이더, 카메라 이동, 품질 전환·저장·복원, 탭 비가시·복귀, Weather 캐시·강제 새로고침을 포함합니다.
