# Route of Sky 실무 작업 가이드

이 문서는 저장소에서 작업하는 에이전트와 기여자가 안전하고 재현 가능한 변경을 만들기 위한 공통 기준이다. 절차 자체보다 사용자 의도, 변경 범위, 검증 근거를 우선하며 작업 성격에 맞는 수준으로 적용한다.

## 1. 규칙 우선순위

1. 시스템·플랫폼 등 상위 실행 환경의 지시를 따른다.
2. 사용자가 명시한 목표, 범위, 금지사항, 브랜치명을 따른다.
3. 이 문서와 저장소의 기존 관례를 따른다.
4. 규칙이 충돌하면 더 구체적이고 최근의 지시를 우선하고, 안전이나 결과에 영향을 주는 충돌은 사용자에게 알린다.

## 2. 기본 작업 원칙

- 구현 전에 현재 브랜치, 작업 트리, 관련 코드, 테스트, 문서를 확인한다.
- 기존 변경과 새 파일은 사용자 소유로 간주하며, 요청과 무관한 내용을 수정하거나 되돌리지 않는다.
- 목표를 충족하는 가장 작은 변경을 선택하고, 추측보다 코드·로그·테스트·측정 결과를 근거로 판단한다.
- 읽기 전용 조사와 검증은 적극적으로 수행하되, 삭제·덮어쓰기·배포·외부 전송처럼 영향이 큰 작업은 명확한 권한과 대상을 확인한다.
- API, 사용자 동작, 시각 품질을 바꾸는 경우 요청 범위를 넘어서는 부수 변경을 함께 넣지 않는다.
- 실패한 시도와 목표 미달 결과를 숨기지 않는다. 유지하지 않는 구현은 안전하게 제거하고 판단 근거를 기록한다.

## 3. Git과 브랜치

- `main`에는 직접 커밋하거나 푸시하지 않는다.
- 새 작업은 최신 `main`에서 작업 브랜치를 만든다. 기본 접두사는 다음과 같다.
  - 기능: `feature/<작업명>`
  - 버그 수정: `fix/<작업명>`
  - 리팩터링: `refactor/<작업명>`
  - 테스트: `test/<작업명>`
  - 문서: `docs/<작업명>`
  - 성능: `perf/<작업명>`
- 사용자가 브랜치명을 지정했다면 해당 이름을 우선한다.
- 한 브랜치에는 함께 설명하고 검증할 수 있는 하나의 목적만 담는다. 동일 목적의 테스트·문서·설정 변경은 함께 포함할 수 있다.
- 커밋은 검증 가능한 논리 단위로 나누고, 커밋 메시지에는 변경 의도가 드러나게 작성한다.
- `git reset --hard`, 광범위한 `git checkout --`, 강제 푸시 등 복구가 어렵거나 다른 변경을 잃을 수 있는 명령은 명시적 요청 없이 사용하지 않는다.
- 보호 규칙이나 외부 승인이 필요한 경우 우회하지 않는다.

## 4. 작업 유형별 기준

### 기능 추가

- 기존 구조와 데이터 흐름을 먼저 확인하고 공개 인터페이스 변경을 최소화한다.
- 정상 흐름뿐 아니라 로딩, 빈 상태, 오류, 복구 흐름을 함께 다룬다.
- 새 사용자 동작에는 해당 범위의 단위 테스트와 필요한 E2E 시나리오를 추가한다.

### 버그 수정

- 재현 조건과 원인을 확인한 뒤 수정한다.
- 가능하면 수정 전 실패하고 수정 후 통과하는 회귀 테스트를 남긴다.
- 증상만 가리는 timeout 증가, 예외 무시, 무조건 fallback은 사용하지 않는다.

### 리팩터링

- 사용자 동작과 공개 인터페이스를 유지한다.
- 구조 변경과 기능 변경을 섞지 않는다. 불가피하면 변경 이유와 검증 범위를 분명히 기록한다.
- 변경 전후에 동일한 테스트가 통과하는지 확인한다.

### 테스트 안정화

- 사용자가 제품 코드 변경을 금지하면 테스트 코드, mock, fixture, 테스트 설정만 수정한다.
- 실제 구현의 현재 계약을 먼저 읽고, 테스트 더블이 사용하는 속성·이벤트·비동기 흐름을 정확히 모사하게 한다.
- 실패를 숨기기 위한 `skip`, 의미 없는 assertion, 과도하게 느슨한 matcher, 무분별한 timeout 증가는 금지한다.
- 외부 API는 route 또는 fetch mock으로 격리하고 실제 네트워크 상태에 의존하지 않는다.
- 시간, 난수, animation frame, storage, viewport는 테스트 안에서 결정적으로 제어하고 테스트 사이에 상태가 새지 않게 한다.
- E2E locator는 접근성 role, label, name, test id 순으로 안정적인 사용자 계약을 우선한다. 레이아웃이나 CSS 선택자에 과도하게 결합하지 않는다.
- 먼저 실패 범위의 targeted 테스트를 실행하고, 수정 후 전체 유닛 테스트와 관련 E2E를 실행한다. 유닛과 E2E를 함께 안정화하는 작업은 두 전체 스위트를 모두 실행한다.
- 통과율 100%와 코드 커버리지 100%는 다른 지표다. 결과를 보고할 때 각각 구분한다.

### 문서 변경

- 코드, 명령, 경로, 수치는 저장소의 현재 상태와 대조한다.
- 존재하지 않는 스크립트나 이미 제거된 인터페이스를 예시로 남기지 않는다.
- 문서만 바뀌었다면 앱 전체 테스트를 기계적으로 요구하지 않고 링크·형식·diff를 우선 검증한다.

### 성능 작업

- 실제 trace 또는 측정 결과로 병목을 확인한 뒤 하나의 검증 가능한 가설을 적용한다.
- 평균 FPS만으로 판단하지 않고 프레임 p95, 기능 정확성, 시각 품질을 함께 평가한다.
- High 품질의 기준 시각 효과는 사전 합의 없이 낮추지 않는다. Medium/Low 조정도 수치와 시각 검증 근거가 있을 때만 유지한다.
- 목표를 달성하고 다른 시나리오가 5% 이상 회귀하지 않으며 기능 검증을 통과할 때만 구현을 유지한다.
- 조건을 충족하지 못하면 기능 코드는 롤백하고 실패 원인, 대안, 롤백 사실을 성능 문서에 남긴다.

## 5. 검증 매트릭스

변경과 직접 관련된 검증부터 실행하고, 영향 범위가 넓을수록 단계적으로 확장한다.

| 변경 유형        | 필수 검증                                                                  |
| ---------------- | -------------------------------------------------------------------------- |
| 문서             | 경로·링크·명령 확인, `git diff --check`                                    |
| 테스트·순수 로직 | targeted 테스트, `pnpm test:unit`, `pnpm build`                            |
| Vue UI·상태 흐름 | 관련 단위 테스트, `pnpm test:unit`, 관련 Playwright E2E, `pnpm build`      |
| 전체 사용자 흐름 | `pnpm test:e2e`, 실제 브라우저에서 핵심 화면과 콘솔 오류 확인              |
| 커버리지 변경    | `pnpm test:coverage` 실행 후 통과율과 커버리지 수치를 분리 보고            |
| 성능             | `pnpm build`, `pnpm perf:budget`, 실제 GPU 측정, High/Medium/Low 시각 검증 |

- targeted Vitest는 `pnpm exec vitest run <테스트 파일>` 형식을 사용한다.
- targeted Playwright는 `pnpm exec playwright test <테스트 파일 또는 -g 패턴>` 형식을 사용한다.
- `pnpm lint`는 자동 수정 명령이다. 검증만 필요할 때는 `pnpm exec eslint <대상>`을 사용해 사용자 파일을 불필요하게 다시 쓰지 않는다.
- 환경 제약으로 검증하지 못한 항목은 통과한 것처럼 보고하지 않고, 실패 원인과 미검증 범위를 명시한다.
- 기능 검증이 필요한 경우 지역 변경, 시간 슬라이더, 카메라 이동, 품질 전환·저장·복원, 탭 비가시·복귀, Weather 캐시·강제 새로고침 중 변경과 관련된 시나리오를 선택한다.

## 6. 성능 측정 규칙

- 공식 GPU 수치는 실제 GPU가 활성화된 로컬 Chrome/Chromium에서 1365×768, HTTP 캐시 비활성화, 동일 시나리오 3회 중앙값으로 확정한다.
- 데스크톱과 CPU ×4 저사양 결과를 분리한다.
- SwiftShader, llvmpipe 등 software renderer 결과는 공식 수치로 사용하지 않는다.
- 외부 타일과 Weather API 변동은 p50/p95로 관찰하되 단독 회귀 판정 근거로 사용하지 않는다.
- 시간·용량·호출 수 개선율은 `(Before - After) / Before × 100`으로 계산한다.
- 성능 표에는 Before, After, 절대 차이, 개선율, 목표 판정을 기록한다.

## 7. 작업 일지와 문서화

- 성능, 보안, 데이터 마이그레이션, 아키텍처 결정, 복잡한 장애 수정에는 작업 일지를 반드시 남긴다.
- 일반 작업도 여러 선택지의 판단 근거, 측정 결과, 롤백 조건을 보존할 가치가 있으면 `docs/worklogs/<작업명>.md`에 기록한다.
- 단순 오탈자나 영향이 작은 기계적 수정에는 별도 작업 일지를 강제하지 않는다.
- 새 작업 일지는 `docs/worklogs/_template.md`를 기준으로 작성하되 해당하지 않는 항목은 억지 수치로 채우지 않는다.
- 성능 기록은 시기별 폴더로 나누지 않고 `docs/performance/`에 연속해서 관리한다.
  - `runs/`: Before/After 원본 JSON과 중앙값
  - `assets/`: 동일 조건의 시각 캡처와 차트
  - `comparison.md`: 전체 수치 비교
  - `measurement-protocol.md`: 재현 조건
  - `optimization-log.md`: 시간순 실험과 판단
  - `case-study.md`: 공개 사례 연구
- 성능 작업 일지는 `docs/worklogs/performance-optimization.md`에 이어 쓰고 가설, 수치, 판정, 롤백 조건을 남긴다.

## 8. 보안과 개인정보

- API 키, 토큰, 비밀번호, 환경 변수 값, 사용자 식별자와 위치를 코드·로그·문서·스크린샷·PR에 기록하지 않는다.
- 원시 URL 쿼리와 원시 GPU 모델·드라이버 정보처럼 식별 또는 비밀 노출 가능성이 있는 값은 공유 자료에서 제거하거나 일반화한다.
- 클라이언트 번들에 서버 전용 비밀을 넣지 않는다. 공개 가능한 환경 변수와 서버 전용 변수를 구분한다.
- 개발 전용 성능 수집은 로컬 개발 환경에서만 활성화하고 Preview·Production 사용자 정보를 수집·저장·전송하지 않는다.
- 외부 서비스 오류를 사용자에게 그대로 노출하지 않고 필요한 범위로 정규화한다.

## 9. PR과 완료 보고

- PR 본문은 한국어로 목표, 주요 변경, 테스트 결과, 위험, 롤백 조건을 포함한다.
- Before/After는 성능, 용량, 호출 수, 통과율처럼 실제로 측정 가능한 변경에만 작성한다. 측정하지 않은 수치를 만들지 않는다.
- UI 변경에는 필요한 경우 동일 조건의 전후 캡처를 첨부한다.
- 자체 리뷰에서는 diff 범위, 테스트 근거, 문서 누락, 비밀정보 포함 여부, 불필요한 생성 파일을 확인한다.
- 최종 보고에는 변경한 내용, 실행한 검증과 결과, 남은 경고 또는 미검증 항목을 구분해서 적는다.
- 푸시, PR 생성, 병합은 사용자의 요청 또는 명확한 작업 범위 안에서 수행하며, 병합 방식이 지정되지 않았다면 저장소의 기존 방식을 따른다.
