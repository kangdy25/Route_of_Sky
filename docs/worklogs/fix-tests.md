# 작업 일지: 테스트 안정화

## 기본 정보

- 날짜: 2026-09-09
- 브랜치: `refactor/fix-test`
- 관련 PR: 없음
- 목표: 제품 코드를 변경하지 않고 현재 유닛 테스트와 E2E 테스트를 모두 통과시킨다.

## 기준선과 가설

- 기준선: 유닛 테스트 280개 중 260개 통과·20개 실패, E2E 테스트 10개 중 7개 통과·3개 실패
- 가설: 테스트 더블과 접근성 locator를 현재 제품 동작 계약에 맞추면 제품 코드 변경 없이 전체 테스트를 통과시킬 수 있다.
- 성공 기준: `pnpm test:unit`과 `pnpm test:e2e`에서 실패 0건

## 변경 내용

- 유닛 테스트 실행 명령을 단발 실행 모드로 변경했다.
- 화면 날씨 렌더러 테스트의 타이머 mock을 현재 `requestAnimationFrame` 기반 구현에 맞췄다.
- Canvas와 Cesium Viewer 테스트 더블에 현재 구현이 사용하는 속성을 추가했다.
- 제거된 초기 카메라 함수와 과거 줌 한계값을 검사하던 기대값을 현재 공개 함수와 설정값에 맞췄다.
- E2E 버튼 locator와 날씨 동기화 상태 문구를 현재 접근성 이름 및 UI 계약에 맞췄다.

## 검증

- 실행 명령: `pnpm test:unit`
- 테스트 결과: 36개 파일, 280개 테스트 모두 통과
- 실행 명령: `pnpm test:e2e`
- 테스트 결과: Chromium E2E 10개 모두 통과
- 실행 명령: 로컬 Vite 서버 브라우저 점검
- 테스트 결과: 본문 렌더링 확인, Vite 오류 오버레이 없음, 브라우저 콘솔 오류 없음, 주요 대시보드 UI 렌더링 확인
- 실행 명령: `pnpm build`, `pnpm test:perf-scripts`
- 테스트 결과: 프로덕션 빌드 성공, 별도 Node 테스트 3개 모두 통과
- 실행 명령: `pnpm test:coverage`
- 테스트 결과: 유닛 테스트 280개 모두 통과, 라인 커버리지 95.0%
- 실행 명령: 변경된 테스트 파일 대상 `pnpm exec eslint ...`
- 테스트 결과: 오류 0건 (기존 Prettier 경고 13건)
- Before → After: 유닛 260/280 (92.9%) → 280/280 (100%), E2E 7/10 (70%) → 10/10 (100%)

## 판단과 롤백

- 판정: 성공. 제품 소스 변경 없이 테스트 코드와 테스트 실행 설정만 현재 계약에 맞췄다.
- 위험: 접근성 이름이나 상태 문구가 의도적으로 변경되면 해당 E2E locator와 기대값도 함께 갱신해야 한다.
- 롤백 조건: 변경된 테스트가 실제 제품 회귀를 숨기거나 현재 UI 계약과 다르게 통과하는 사례가 확인될 때 롤백한다.
- 다음 작업: 필요 시 커밋 및 PR을 진행한다.
