# 작업 일지: study-docs

## 기본 정보

- 날짜: 2026-08-29
- 브랜치: 현재 작업 브랜치
- 관련 PR: 없음
- 목표: Route of Sky의 실제 Vue·Cesium·서버리스 API 구조를 기반으로 한 학습 문서를 추가한다.

## 기준선과 가설

- 기준선: 저장소에는 README와 성능 문서는 있으나, Vue·Cesium 학습 주제별 문서는 분리되어 있지 않다.
- 가설: 실제 파일 경로와 데이터 흐름을 함께 기록하면 프로젝트 코드 탐색과 면접 복습에 재사용할 수 있다.
- 성공 기준: `docs/study/`에 요청된 7개 주제가 분리되어 있고, 코드 변경 없이 문서 형식 검증을 통과한다.

## 변경 내용

- `docs/study/`에 프로젝트 구조, Vue, Cesium, API 흐름, 면접 질문, 복습 노트, 심화 학습 문서를 추가했다.
- 실제 구현 파일과 책임, API·상태 흐름, 성능 품질 제어를 문서화했다.

## 검증

- 실행 명령: `git diff --check`, 문서 파일 목록 확인
- 테스트 결과: 문서 전용 변경이므로 애플리케이션 테스트는 실행하지 않음.
- Before → After: 학습 문서 0개 → 주제별 Markdown 7개.

## 판단과 롤백

- 판정: 문서 추가만 수행했으며 애플리케이션 코드·설정·의존성은 변경하지 않았다.
- 위험: 실제 구현이 바뀌면 문서의 파일 흐름과 수치가 오래될 수 있다.
- 롤백 조건: 학습 문서의 설명이 실제 코드와 불일치한다고 확인될 때 해당 문서를 수정하거나 제거한다.
- 다음 작업: 기능 변경 시 관련 학습 문서의 경로와 흐름을 함께 점검한다.
